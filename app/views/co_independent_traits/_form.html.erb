<%= form_for(@co_independent_trait) do |f| %>
  <% if @co_independent_trait.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@co_independent_trait.errors.count, "error") %> prohibited this co_independent_trait from being saved:</h2>

      <ul>
      <% @co_independent_trait.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contents %><br>
    <%= f.text_area :contents %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
